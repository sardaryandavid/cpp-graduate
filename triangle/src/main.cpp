// #include "triangle.h"

/* Хранить вектор пар: треугольник и признак того, найдено ли пересечение до этого */

int main() {
  #if 0
  int N;
  std::cin >> N;

  std::vector<geometry::triangle_t> tarr;
  #endif
}